# frontend — Domain Index

Route here for: web UI — components, state management, rendering performance, forms,
accessibility, browser behavior.

No pages yet. Grow this domain via `skills/ingest/SKILL.md`.

Planned categories (create on first ingest that fits):

| Category | Will cover |
|----------|-----------|
| state-management | Local vs server state, derived state, cache libraries |
| rendering-performance | Re-render triggers, memoization criteria, list virtualization |
| forms-and-validation | Controlled inputs, validation timing, error display |
| data-fetching | Loading/error states, race handling, retries, optimistic updates |
| accessibility | Semantics, focus management, keyboard paths |
| security | XSS-safe rendering, untrusted content handling (see also security domain) |
