# mobile — Domain Index

Route here for: iOS/Android/cross-platform apps — lifecycle, offline behavior,
release/store concerns.

No pages yet. Grow this domain via `skills/ingest/SKILL.md`.

Planned categories (create on first ingest that fits):

| Category | Will cover |
|----------|-----------|
| lifecycle | App/activity/scene lifecycle, state restoration, background limits |
| offline-and-sync | Local persistence, conflict resolution, retry queues |
| networking | Mobile-network realities: timeouts, radio wakeups, payload budgets |
| release | Store review constraints, staged rollouts, hotfix strategy |
| performance | Startup time, memory pressure, battery-aware work scheduling |
