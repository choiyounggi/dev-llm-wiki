# security — Domain Index

Route here for: handling user input, authn/authz, secrets, dependencies, or anything
crossing a trust boundary.

No pages yet. Grow this domain via `skills/ingest/SKILL.md`.

Planned categories (create on first ingest that fits):

| Category | Will cover |
|----------|-----------|
| input-handling | Validation at trust boundaries, parameterization, encoding on output |
| authn-authz | Session/token handling, permission checks at the resource level |
| secrets | Storage, rotation, keeping secrets out of code/logs/history |
| dependencies | Supply-chain hygiene, update strategy, lockfiles |
| data-protection | PII handling, encryption at rest/in transit, erasure requirements |
